// c system
#include <cfloat>
#include <cmath>
#include<spawn_car/utils.h>
#include<nav_msgs/Odometry.h>
namespace utils {


geometry_msgs:Vector3  calc_angular(const double& va_x, const double& va_y, const double& va_z) {
  double a{0.0};
  if (std::abs(d) < DBL_EPSILON) {
    a = std::numeric_limits<double>::infinity();
  } 
  return a;
}
geometry_msgs:Vector3  calc_linear(const double& vl_x, const double& vl_y, const double& vl_z) {
  double a{0.0};
  if (std::abs(d) < DBL_EPSILON) {
    a = std::numeric_limits<double>::infinity();
  } 

  return a;
}


} //namespace utils